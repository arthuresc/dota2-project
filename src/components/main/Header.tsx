import React from 'react';
import { Button } from "antd";

type Props = {}

function Header({}: Props) {
  return (
    <div>
      Header
      <Button>Home</Button>
    </div>
  )
}

export default Header;